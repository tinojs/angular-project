export interface UserForAuthentication {
  username: string;
  email: string;
  password: string;
  tel: string;
  id: string;
}

export interface ProfileData {
  username: string;
  email: string;
  tel: string;
}